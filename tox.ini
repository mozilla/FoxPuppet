[tox]
skipsdist = true
envlist = py{27,35}

[testenv]
deps =
    pytest==3.0.3
	pytest-selenium==1.4.0
	selenium==3.0.0b2
commands = pytest --driver Firefox foxpuppet/tests {posargs}

[pytest]
testpaths = foxpuppet/tests
